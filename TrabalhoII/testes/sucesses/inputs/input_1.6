class retangulo{

real base;
real altura;

retangulo(real b, real a){
base=b;
altura=a;
}

fun real area(){
return base * altura;
}

fun real perimetro(){
return 2* (base + altura);
}

ret = new retangulo(17,9);

real area,per;

area = ret.area();
per = ret.perimetro();

area;
per;
